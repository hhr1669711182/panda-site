{
  "name": "@panda/prisma",
  "version": "1.0.0",
  "description": "Prisma client for panda-site",
  "main": "./index.ts",
  "scripts": {
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "seed": "tsx seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "tsx": "^4.19.0"
  }
}
